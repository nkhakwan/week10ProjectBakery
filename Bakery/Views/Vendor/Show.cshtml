@using Bakery.Models;

@{
  Layout = "_Layout";
}

<h1>Here are all the Orders for this Vendor:</h1>

@* <ol>  Keeping commented out lines for future reference*@
@foreach (Order order in @Model["order"])
{
  <h4>Title: @order.Title, Description: @order.Description,  Price: @order.Price, Date: @order.Date</h4>
}
@* </ol> *@

<button class="btn btn-light"><a href='/vendors/@Model["vendor"].Id/orders/new'>Add a new order</a></button>
@* <br> *@
@* <button class="btn btn-light"><a href='/orders/delete'>Delete all orders</a></button> *@
<br> 
<button class="btn btn-light"><a href='/vendors'>Return to vendors list</a></button>
<br>
<button class="btn btn-light"><a href='/'>Return to Main Page</a></button>






@*<h1>Here are all the Orders from Current Vendor</h1>
@if (Model.Count == 0)
{
<h3>This Vendor Never Ordered Before</h3>
}
@foreach (Order order in Model)
{
<p>@order.Title @order.Description @order.Price @order.Date</p>
}
<br>
<br>
<button class="btn btn-light"><a href='/orders/new'>Add a new Order</a></button>
<button class="btn btn-light"><a href='/vendors'>Go back to Vendor's Page</a></button>*@
